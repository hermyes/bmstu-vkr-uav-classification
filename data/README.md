# Data Directory

Директория `data/` зарезервирована под будущие выгрузки и внешние источники данных.

На текущем этапе основной датасет хранится в:

- `train_sounds/dataset_out` (готовые `train/val/test` + `manifest.json`)

Если добавляются новые наборы данных, рекомендуется складывать краткие описания источников именно в этот каталог.
